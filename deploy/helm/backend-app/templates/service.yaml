apiVersion: v1
kind: Service
metadata:
  name: backend-app
spec:
  type: {{ .Values.service.type }}
  selector:
    app: backend-app
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
